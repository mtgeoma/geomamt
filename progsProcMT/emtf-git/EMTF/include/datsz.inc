ccc    INCLUDE FILE ../../datsz.inc
ccc
ccc    FOR EMTF (dnff, tranmt, multmtrn) 
ccc

c******parameters to change for output file format
c...... number of data samples to put in one output record
       include '../include/nchmx.inc'
       parameter(nt0 = 256,nblk = nt0)
